:11434 {
    # 同步服务分流：探测到路径包含 /api/ 则直接转发给后端服务
    handle /api/* {
        reverse_proxy yiboflow_api:8080
    }

    # 兜底处理 (防止误访问其他路径)
    handle {
        respond "YiboFlow Sync Gateway is Active" 200
    }
}
